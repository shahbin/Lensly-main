<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!--=============== FLATICON ===============-->
    <link
      rel="stylesheet"
      href="https://cdn-uicons.flaticon.com/2.0.0/uicons-regular-straight/css/uicons-regular-straight.css"
    />

    <!--=============== SWIPER CSS ===============-->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"
    />

    <!--=============== CSS ===============-->
    <link rel="stylesheet" href="/css/style.css">

    <title>Lensly</title>
  </head>
  <body>
    <!--=============== HEADER ===============-->
    <header class="header">
      <nav class="nav container">
        <a href="/" class="nav__logo">
          <img
            class="nav__logo-img"
            src="/assets/img/lensly logo.png"
            alt="website logo"
          />
        </a>
        <div class="nav__menu" id="nav-menu">
          <div class="nav__menu-top">
            <a href="/" class="nav__menu-logo">
              <img src="/assets/img/lensly logo.png" alt="">
            </a>
            <div class="nav__close" id="nav-close">
              <i class="fi fi-rs-cross-small"></i>
            </div>
          </div>
          <ul class="nav__list">
            <li class="nav__item">
              <a href="/" class="nav__link active-link">Home</a>
            </li>
            <li class="nav__item">
              <a href="/shop" class="nav__link">Shop</a>
            </li>
            <li class="nav__item">
              <a href="contact" class="nav__link">Contact Us</a>
            </li>
            <li class="nav__item">
              <a href="about" class="nav__link">About Us</a>
            </li>
          </ul>
          <div class="header__search">
            <form action="/shop" method="GET" class="search-form">
              <% if (typeof selectedCategory !== 'undefined' && selectedCategory && selectedCategory.length > 0) { %>
                <input type="hidden" name="category" value="<%= selectedCategory.join(',') %>">
              <% } %>
              <% if (typeof selectedSort !== 'undefined' && selectedSort) { %>
                <input type="hidden" name="sort" value="<%= selectedSort %>">
              <% } %>
              <% if (typeof minPrice !== 'undefined') { %>
                <input type="hidden" name="minPrice" value="<%= minPrice %>">
              <% } %>
              <% if (typeof maxPrice !== 'undefined') { %>
                <input type="hidden" name="maxPrice" value="<%= maxPrice %>">
              <% } %>
              
              <div class="search-box" style="
                display: flex;
                align-items: center;
                border: 1px solid #ddd;
                border-radius: 25px;
                overflow: hidden;
                width: 300px;
                background: white;
              ">
                <input
                  type="text"
                  name="search"
                  value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
                  placeholder="Search For Items..."
                  style="
                    border: none;
                    padding: 8px 15px;
                    flex: 1;
                    outline: none;
                    font-size: 14px;
                  "
                />
                <button 
                  type="submit" 
                  style="
                    border: none;
                    background: none;
                    padding: 8px 15px;
                    cursor: pointer;
                  "
                >
                  <i class="fi fi-rs-search" style="color: #666; font-size: 16px;"></i>
                </button>
              </div>
            </form>
          </div>
        </div>
        <div class="header__user-actions">
          <a href="/wishlist" class="header__action-btn" title="Wishlist">
            <img src="/assets/img/icon-heart.svg" alt="" />
            <!-- <% if (user) { %>
            <span class="count"><%= typeof wishlistItems !== 'undefined' ? wishlistItems.length : 0 %></span>
            <% } %> -->
          </a>
          <a href="/cart" class="header__action-btn" title="Cart">
            <img src="/assets/img/icon-cart.svg" alt="" />
            <!-- <% if (user) { %>
              <span class="count"><%= typeof cartItems !== 'undefined' ? cartItems.length : 0 %></span>
            <% } %> -->
          </a>
          <div class="header__container">
            <% if (user) { %>
              <div class="dropdown">
                <a href="#" class="sign-in-link" style="margin-left: 50px;"><%= user.name %></a>
                <div class="dropdown-content">
                  <a href="/userProfile">Profile</a>
                  <a href="/logout">Logout</a>
                </div>
              </div>
            <% } else { %>
              <a href="/login" class="header__top-action">
                Log In  
              </a>
            <% } %>
          </div>
          <div class="header__action-btn nav__toggle" id="nav-toggle">
            <img src="/assets//img/menu-burger.svg" alt="">
          </div>
        </div>
      </nav>
    </header>
  </body>
</html>